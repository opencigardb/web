import { PlaceholderPage } from "@/components/placeholder-page";

export default function CigarNotFound() {
  return (
    <PlaceholderPage
      title="Cigar not found"
      blurb="This record doesn't exist — it may have been merged or the link is incorrect."
    />
  );
}
